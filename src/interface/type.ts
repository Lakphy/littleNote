export interface quickNoteType {
  title: string;
  content: string;
}
export interface linkType {
  title: string;
  content: string;
}
export interface photoType {
  title: string;
  content: string;
}

export enum cards {
  quickNoteType,
  linkType,
  photoType,
}
